/*
 * CoronaStatePage Messages
 *
 * This contains all the text for the CoronaStatePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CoronaStatePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CoronaStatePage container!',
  },
});
