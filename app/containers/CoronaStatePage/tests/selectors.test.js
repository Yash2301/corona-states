// import { selectCoronaStatePageDomain } from '../selectors';

describe('selectCoronaStatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
